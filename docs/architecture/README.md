# Architecture

The approved runtime design lives in
`.scratch/smart-smile-pwa/architecture.md`. This directory is the tracked home for implementation
architecture decisions as the PWA grows. Ticket 01 contains only the static React shell and the
preserved Python reference; camera, inference, capture, and service-worker lanes remain future work.
